# Environments

Environment definitions and local learning-environment notes belong here.
Disposable Docker-based debug labs currently live under `labs/debug-labs/`.

The Proxmox inventory example lives under `ansible/`; local inventories,
credentials, Terraform variables, and runtime state must remain untracked.
